/**
 * AcadeGrade Service Worker — Serwist 9.5.11
 *
 * This file will be compiled by @serwist/next when PWA integration
 * is enabled in next.config.ts (Phase 10).
 *
 * Caching strategy:
 * - Precache: all static assets from Next.js build manifest
 * - Runtime: NetworkFirst for API calls, StaleWhileRevalidate for images
 * - Offline: serves cached pages when network is unavailable
 *
 * Implementation deferred to Phase 10 to avoid build issues before
 * Serwist is properly configured in next.config.ts.
 */

export {};
